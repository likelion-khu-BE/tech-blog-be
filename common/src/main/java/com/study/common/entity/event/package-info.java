/**
 * 세션보드 팀 — 이벤트/일정 성격 엔티티 영역.
 *
 * <p>예상 엔티티: {@code EventPost}, 일정·시간 기반 도메인.
 *
 * <p>ArchUnit 규칙의 와일드카드 매칭 회피 위해 팀명({@code sessionboard}) 대신 도메인명({@code event})을 서브패키지 이름으로 사용.
 */
package com.study.common.entity.event;
