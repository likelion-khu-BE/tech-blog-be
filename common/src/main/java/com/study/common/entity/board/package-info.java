/**
 * 세션보드 팀 — 게시판(post) 성격 엔티티 영역.
 *
 * <p>예상 엔티티: {@code SessionNote}, {@code Resource} 등 "정보 공유·기록" 성격의 도메인.
 *
 * <p>ArchUnit 규칙의 와일드카드 매칭 회피 위해 팀명({@code sessionboard}) 대신 도메인명({@code board})을 서브패키지 이름으로 사용.
 */
package com.study.common.entity.board;
